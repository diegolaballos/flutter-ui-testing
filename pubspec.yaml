name: ui_testing
description: A new Flutter project.
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  #Flutter driver
  flutter_driver:
    sdk: flutter
  test: any

flutter:
  uses-material-design: true
